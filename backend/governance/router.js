const express=require('express');const pool=require('../db');const auth=require('../middleware/auth');const {createWorkflow}=require('./workflowCore');const {createGovernedRouter}=require('./routerFactory');const db={query:async(s,p)=>(await pool.query(s,p)).rows,transaction:async work=>{const c=await pool.connect();try{await c.query('BEGIN');const r=await work(async(s,p)=>(await c.query(s,p)).rows);await c.query('COMMIT');return r;}catch(e){await c.query('ROLLBACK');throw e;}finally{c.release();}}};module.exports=createGovernedRouter({express,workflow:createWorkflow(require('./config')),auth,db});
